<Window x:Class="ckir_crasher_2.Windows.Activation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ckir_crasher_2.Windows"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:fa="http://schemas.fontawesome.com/icons/fonts"
        mc:Ignorable="d"
        Title="activation" Height="300" Width="700"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">
    <Border Background="#fff" CornerRadius="5" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="200"/>
            </Grid.RowDefinitions>

            <!-- Title Section -->
            <Grid Grid.Row="0" MouseLeftButtonDown="Grid_MouseLeftButtonDown" Grid.ColumnSpan="2">
                <TextBlock VerticalAlignment="Center" FontSize="18" Margin="15,0,0,0" FontFamily="/Fonts/#a고딕17">
                    <fa:FontAwesome Icon="Solid_Key"/> 라이센스 등록 매니저
                </TextBlock>
                <Button Name="closeapp" HorizontalAlignment="Right" Width="50" Background="Transparent" BorderBrush="Transparent" Click="closeapp_Click">
                    <Button.Content>
                        <fa:FontAwesome Icon="Solid_Xmark" FontSize="18"/>
                    </Button.Content>
                </Button>
            </Grid>

            <!-- Alert Section -->
            <Grid Grid.Row="1" Grid.ColumnSpan="2">
                <TextBlock VerticalAlignment="Center" FontSize="14" Margin="15,0" FontFamily="/Fonts/#a고딕13">
                    귀하의 계정에 등록된 라이센스 키가 없습니다. 라이센스를 등록해주세요.
                    <LineBreak/>
                    <Run Foreground="#ff9f9f">라이센스를 등록하면 계정에 귀속됩니다. 계속하기 전에 올바른 계정으로 로그인 되어있는지 확인해주세요.</Run>
                </TextBlock>
            </Grid>

            <!-- main section -->
            <Grid Grid.Row="2" Grid.ColumnSpan="2">
                <StackPanel Margin="15,0" VerticalAlignment="Center">
                    <hc:TextBox Name="textbox_serial" Margin="0,0,0,10" hc:InfoElement.Placeholder="280a8a4d-a27f-4d01-b031-2a003cc4c039"
               hc:InfoElement.Title="라이센스 키" FontFamily="/Fonts/#a고딕13"/>
                    <Button Name="button_Activate" HorizontalAlignment="Stretch" Style="{StaticResource ButtonPrimary}" FontFamily="/Fonts/#a고딕13" Click="button_Activate_Click">
                        등록하기
                    </Button>

                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</Window>
